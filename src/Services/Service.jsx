import { Button } from "reactstrap";
import Base from "../components/Base";

const Service=()=>{
    return (
        <Base>
        <div>
            <h1> This is done while working !</h1>
            <Button>Button hai</Button>
        </div>
        </Base>
    );
}
export default Service;